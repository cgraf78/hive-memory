# The vendored release entry points source scripts/release-lib.sh next to them,
# and the shell test suite sources scripts/release.conf. Resolving siblings here
# keeps local runs and commit hooks agreeing with CI, which passes
# -x -P SCRIPTDIR explicitly.
external-sources=true
source-path=SCRIPTDIR
