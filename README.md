# hive-memory

Vendor-neutral shared memory infrastructure for AI agents.

The current design plan lives in [PLAN.md](PLAN.md). The goal is to build a configurable, backend-agnostic memory layer that gives agent sessions across machines a shared hive-brain while keeping the canonical store human-readable and conflict-resistant.
